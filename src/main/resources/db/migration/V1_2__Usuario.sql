
CREATE TABLE User (Id BIGINT GENERATED BY DEFAULT AS IDENTITY (INCREMENT BY 1) NOT NULL,
UUID VARCHAR(255), Created_Timestamp TIMESTAMP, Active TINYINT, Email VARCHAR(255) NOT NULL UNIQUE, Password VARCHAR(255),
Last_Login TIMESTAMP, Modified TIMESTAMP, Token VARCHAR(255), PRIMARY KEY (Id));